server:
  port: 8001
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.20.150:3306/myblog?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: root
    password: root
mybatis-plus:
  mapper-locations: classpath:/com/potoyang/learn/blog/*/mapper/*Mapper.xml
  type-aliases-package: com.potoyang.learn.blog.*.model
  global-config:
    db-config:
      id-type: auto
    refresh: true
management:
  endpoints:
    web:
      exposure:
        include: '*'
  metrics:
    tags:
      application: "blogApplication"
config:
  blog-save-dir: F:/blog/